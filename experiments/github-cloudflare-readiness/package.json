{
  "name": "github-cloudflare-readiness-sentinel",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "check:scripts": "node --check scripts/prepare-cloudflare.mjs && node --check scripts/write-runtime-secrets.mjs && node --check scripts/write-test-policy.mjs",
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260823.1",
    "typescript": "^5.9.2",
    "vitest": "^3.2.4",
    "wrangler": "^4.125.0"
  }
}
