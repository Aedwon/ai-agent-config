# Generic Adapter

The generic adapter renders `AGENT_RULES.md` for manual integration. It makes no
automatic discovery claim because generic agents share no universal project
instruction filename.

Render into a staging directory, inspect the file, then configure the target
agent through its documented project mechanism. The template adds no policy and
contains the same canonical bundle as every other adapter.
